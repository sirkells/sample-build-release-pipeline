trigger:
  branches:
    include:
    - main
  paths:
    exclude:
    - pipelines/*
    - azure-pipelines.yml
pool:
  vmImage: 'ubuntu-latest'

stages:
- template: pipelines/build.yml
- template: pipelines/release.yml